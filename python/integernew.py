#enter two collections of integers.check whether they are of same length.whether they sums to the same value.whether any value occur in both
s1=input("entr the collection of integers:")
s2=input("enter the second collection of integers:")

s1=set(map(int s1.split())
s2=set(map(int,s2.split())
       print('lists are of same length:'len(s1)==len(s2))
       
